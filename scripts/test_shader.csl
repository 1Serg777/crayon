ShaderProgram "Test Shader" {

    VertexInputLayout {
        vec4 pos : POSITION;
    }

    ColorAttachments {
        vec4 col : COLOR0;
    }

    VertexShader {
        BEGIN
        // #version 460 core
        /*
        vec2 positions[3] = vec2[](
            vec2(0.0, 0.5),
            vec2(0.5, 0.5),
            vec2(0.5, 0.5)
        );
        */
        /*
        vec2 positions[3] = vec2[](
            vec2( 0.0, -0.5),
            vec2(-0.5,  0.5),
            vec2( 0.5,  0.5)
        );
        */
        /*
        vec2 positions[3] = {
            { 0.0, -0.5},
            {-0.5,  0.5},
            { 0.5,  0.5}
        };
        */
        void main() {
            gl_Position = vec4(pos, 1.0);
        }
        END
    }
    FragmentShader {
        BEGIN
        void main() {
            col = vec4(0.5, 0.5, 0.5, 1.0);
        }
        END
    }

}